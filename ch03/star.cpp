#include <simplecpp>
main_program
{
    turtleSim();
    double vertexAngle = 144;
    int side = 200;
    repeat(5)
    {
        forward(side);
        right(vertexAngle);
    }
    wait(10);

    

    
}